name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install black flake8 isort

      - name: Check formatting with black
        run: black --check netbox_endpoints/

      - name: Check imports with isort
        run: isort --check-only netbox_endpoints/

      - name: Lint with flake8
        # E203/W503 conflict with black's formatting (e.g. slice colons); ignore per black docs
        run: flake8 netbox_endpoints/ --max-line-length=120 --extend-ignore=E203,W503
